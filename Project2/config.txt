# START        -> Starting Size (Ex: START=500 -> N = 250; M = 250)
# STEP         -> Size increment
# INSTANCES    -> Number of instances
# DATA_DIR     -> Directory to process data (must contain the generator code)


START=10000
STEP=10000
INSTANCES=30
DATA_DIR=data
